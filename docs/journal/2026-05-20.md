# 2026-05-20 — overnight scaffold session

## What got done

- Renamed project from BOOST → HEED → Flabby → Glemton after prior-art sweeps blocked each previous name. Glemton is a fully invented word; zero hits across HF, GitHub, web, AI startups, app stores, trademarks, characters, slang, persons, places as of this date.
- Wrote core project scaffold at `C:\Users\jedin\Desktop\Glemton`:
  - `CLAUDE.md` — project bible (honesty rules, attribution rule, locked architecture, training plan, risks)
  - `README.md` — public-facing pitch
  - `LICENSE` — Apache-2.0
  - `pyproject.toml`, `requirements.txt`, `.gitignore`
  - `configs/glemton-350m.yaml`, `configs/debug-10m.yaml`
- Created Python 3.11 venv at `.venv`. Installed:
  - PyTorch 2.12.0.dev20260408+cu128 (nightly)
  - transformers 5.9.0, tokenizers 0.22.2, datasets 4.8.5
  - sentencepiece, wandb, pyyaml, tqdm, einops, numpy, requests, bs4, lxml, regex
  - bitsandbytes attempted but not verified on Windows (Adam-8bit will fall back to AdamW if bnb fails at runtime)
- **Verified PyTorch sees the RTX 5070** — `capability=(12, 0)` (sm_120 / Blackwell), `bf16_supported=True`.
- Wrote model + training code under `src/glemton/`:
  - `model.py` — Llama-style dense transformer (RMSNorm, SwiGLU, GQA, RoPE θ=1e6, sliding-window). Build+forward verified on CPU; debug-10m config → 12.3M params, glemton-350m config → 365M params.
  - `tokenizer.py` — BPE trainer with special tokens `<pad>`, `<bos>`, `<eos>`, `<unk>`, `<user>`, `<reply>`, `<sep>`.
  - `data.py` — `PackedDataset` mmap-friendly iterator over uint16 `.bin` shards.
  - `train.py` — single-GPU loop with bf16 autocast, grad accumulation, cosine LR with token-based warmup, periodic checkpointing.
  - `eval_sycophancy.py` — regex-based marker counter for the first headline claim.
  - `eval_long_conversation.py` — synthetic plant/recall probe builder for the second headline claim.
- Wrote download + prep scripts under `scripts/`:
  - `download_data.py` (v1: gutenberg_dialogue, ubuntu_dialogue, common_pile, stack_exchange, open_subtitles)
  - `download_data_v2.py` (fallbacks for HF dataset-script deprecation)
  - `prepare_corpus.py` (gutenberg_dialogue, stack_exchange, open_subtitles, common_pile)
  - `prepare_gutenberg_books.py` (heuristic dialogue extraction from raw Gutenberg .txt files)
  - `train_tokenizer.py`, `pack_shards.py`, `run_debug_train.py`
- Wrote `tests/probes/sycophancy_probes.txt` — 39 probe prompts for the sycophancy eval.
- Initialized git repo, added remote `https://github.com/ninjahawk/glemton.git`, set `user.name = ninjahawk`. Did not push (no credential setup; user can `git push -u origin main` when ready).

## Data state at end of session

- `data/raw/gutenberg_books/` — 15 classic books (Pride & Prejudice, Sherlock Holmes, Moby-Dick, War & Peace, Anna Karenina, Wuthering Heights, Crime & Punishment, Dracula, Treasure Island, Picture of Dorian Gray, Frankenstein, Heart of Darkness, Tale of Two Cities, Huck Finn, Scarlet Letter). 14 MB total.
- `data/raw/gutenberg_dialogue/source.tar.gz` — the `ricsinaruto/gutenberg-dialog` extractor repo (code only, no pre-extracted data).
- `data/raw/common_pile/` — 20 streamed text shards from Common Pile v0.1 (2.3+ GB).
- `data/raw/stack_exchange/` — 2 of 7 targeted .7z dumps (philosophy + skeptics; the rest 404'd on archive.org). 195 MB.
- `data/raw/open_subtitles/`, `data/raw/daily_dialog/` — **empty**, both HF dataset scripts deprecated and no parquet equivalent worked in the overnight session.

## What did NOT work

- `Helsinki-NLP/open_subtitles` HF dataset: deprecated loader script, no longer supported by `datasets >= 4.x`. Need a community parquet mirror or a direct OPUS-project download.
- `daily_dialog` HF dataset: same — deprecated loader script. There's likely a parquet mirror under a different namespace; haven't found it yet.
- Most of the Stack Exchange `.7z` URLs on `archive.org/download/stackexchange/` 404'd. Only `philosophy.stackexchange.com.7z` and `skeptics.stackexchange.com.7z` are present. Either the archive paths shifted, or the smaller communities migrated to a different bundle. Will need to inspect the Stack Exchange data dump index page.
- `bitsandbytes` install on Windows: appears to have installed (no error), but its native CUDA kernels may not load at runtime on Blackwell — the train loop falls back to plain AdamW if so. To be confirmed only when we run the debug train.

## Next steps (in priority order)

1. Run `scripts/prepare_gutenberg_books.py` to extract dialogue chunks from the 15 books.
2. Run `scripts/prepare_corpus.py common_pile` (queued; may already have run in background).
3. Find a working source for OpenSubtitles or skip it for v1.0 and lean harder on Gutenberg / Common Pile / Stack Exchange.
4. Train the 32k BPE tokenizer once enough processed text is on disk.
5. Pack shards.
6. Run the 10M debug train as the Phase 0 gate.

## Honest reality check

- The toolchain works (PyTorch + Blackwell). That was the single highest risk going in.
- The corpus assembly is harder than I budgeted for. HF deprecating loader scripts blew up two sources. We have ~2.5 GB of raw text on disk; far from the 7B-token target. Either (a) the user picks up the OpenSubtitles + Stack Exchange data acquisition manually, or (b) we reduce the v1.0 token budget and accept a less-converged model.
- Model code, training code, eval code, docs are all written and self-consistent. The remaining work is mostly data, plus the actual training run.
